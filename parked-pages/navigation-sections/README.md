# 暂时下线的导航栏目

按用户要求下线能力、成果、场景、阅读四个导航栏目，以及 capabilities、projects、scenarios、insights、news、archive 页面和详情（包括英文版）。

此目录保存原 Next 路由文件和页面实现快照。文章原稿仍在 articles，产品与场景配置仍保留供后续恢复；当前页面数据、导航、页脚、搜索、RSS 均不展示下线内容。

下线开关集中在 src/data/disabled-sections.ts。恢复时将路由目录移回 src/app，调整开关、恢复首页文章栏目并重新构建。移动目录后的相对引用以原位置为准，暂存源码不参与构建。

## 新闻栏目已恢复

后续按用户要求，news 路由已移回 src/app/news，并作为“最近新闻”独立导航分组。首页恢复最新 6 条新闻，原有新闻原稿、详情、搜索、RSS 同步恢复。其他下线栏目保持暂停。
